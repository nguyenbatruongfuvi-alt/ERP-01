{
  "scripts": {
    "install": "npm install --registry=https://registry.npmjs.org/ --no-audit --no-fund",
    "dev": "vite",
    "build": "vite build && cp manifest.json dist/manifest.json && cp sw.js dist/sw.js"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
